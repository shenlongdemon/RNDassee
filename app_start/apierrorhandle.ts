import { ErrorResult } from "business_core_app_react";

const handleBusinessError = (error: ErrorResult): void => {
}
const handleexceptionError = (error: ErrorResult): void => {
}

export {handleBusinessError, handleexceptionError};